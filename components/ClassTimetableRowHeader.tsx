export const ClassTimetableRowHeader: React.FC = ({ children }) => {
    return (
        <div className="p-4 font-bold bg-gradient-to-r from-purple-700 to-purple-800 uppercase text-white">
            {children}
        </div>
    );
};
